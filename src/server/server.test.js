describe('Test Weather API', function () {

    test('Returns weather for zipcode and time', () => {
      
        let returnedData = {};
        expect(returnedData).toEqual('here we need to see how to make sure the data isnt wrong or undefined');
    });

    
});
